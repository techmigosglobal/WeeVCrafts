package partials

// HTMX fragment boundaries are currently rendered by internal/web/handlers;
// this package is reserved for shared partials as the live adapters arrive.
templ PreviewNotice() { <div class="preview-bar">UI preview · sample data</div> }
